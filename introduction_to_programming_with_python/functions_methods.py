# list_1 = [False, False]
# print(any(list_1))

# list_2 = [False, True]
# print(any(list_2))
# print(all(list_2))

# nums = [1, 2, 3, 4]
# print(any([num % 2 == 0 for num in nums]))
# print(all([num % 2 == 0 for num in nums]))

print(dir(5))

# help

# predicates are functions that always return a bool.
# functions vs methods
# mutating the caller is a characteristic of methods
# functions can mutate arguments